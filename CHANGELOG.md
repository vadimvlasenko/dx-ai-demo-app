```markdown
# Changelog

## [Unreleased]
- Added Badge entity with fields id and name
- Created BadgeRepository for CRUD operations on Badge entities
- Added BadgeController with base path for Badge API endpoints
- Updated application.properties with JPA properties
- Updated DemoApplication to include `@EntityScan` annotation
```